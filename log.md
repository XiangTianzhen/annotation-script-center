# 标注脚本中心修改总账

## 2026-09-02

- 发布(1.1.8): 发布台州话 node 17 内部质检自动提交稳定性修复。单选变更后按行键重定位并最多等待 3 秒确认；确认轮询已接入真实运行时。未尝试提交的暂态失败经同一检查包范围复核后等待 5 秒自动重试，已尝试提交、范围变化及 DOM 歧义继续永久停止，避免重复提交。
- 新增(台州话内部质检): node 17 自动提交在未尝试右上角“提交”的暂态网络、状态确认或控件暂不可用失败时，会先显示失败并在同一检查包范围核验通过后等待 5 秒自动重新开始；面板显示“等待自动重试”和累计次数，用户可点击“停止”取消。已尝试提交、路由/检查包变化、行或控件歧义仍永久停止，避免重复提交。
- 测试(台州话内部质检): 覆盖未确认分段 5 秒后自动重跑、网络等待失败进入重试且停止可取消，以及已尝试提交但未确认下一题不安排重试。
- 修复(台州话内部质检): 修正 node 17 运行时创建逐段修正流程时遗漏传入“单选状态确认轮询”的接线错误；此前真实页面会在首次 DOM 读取仍未更新时直接显示“内部质检变更后未确认”，未实际执行既有 3 秒轮询。现在确认轮询及点击后动作时间均由控制器传入，超时才显示“点击后 3000ms 未确认”。
- 测试(台州话内部质检): 新增运行时至逐段修正的确认轮询/动作时间转交回归，防止控制器已有等待能力却未接入真实页面流程。
- 修复(台州话内部质检): node 17 分段“合格”受限鼠标输入已发出但 AIDP 状态延迟更新时，自动流程不再在首次读取未选中后立即停止；现在按当前行键最多只读轮询 3 秒，确认“合格”后继续，期间不发送第二次点击。超时仍未确认则显示对应分段和等待时长并安全停止。
- 测试(台州话内部质检): 覆盖单次点击后的延迟状态更新及控制器在等待确认期间不提交、确认后才提交的流程。
- 新增(shujiajia): 正式注册数加加泸州话助手、Popup、Options、Manifest、storage schema 37 和统一后端三项 AI 接口；脚本及八项快捷键默认关闭/为空，合并远端发布提交后扩展版本保持 `1.1.8`。
- 新增(shujiajia): 接入 iframe B 布局、零段落整音频单段划分、两阶段泸州话识别、人工填入、当前段有效性、原生暂存和受待暂存状态保护的提交快捷键；面板不提供提交按钮。
- 安全(shujiajia): 音频响应字节仅在页面内存转为 `audioDataUrl`，不保存或记录真实 URL、签名参数及凭证；已有段落、节点歧义、边界校验或失败回滚异常均停止人工处理，不直接调用平台写接口。
- 安全(shujiajia): 音频、待暂存状态和成功响应按 `taskId + dataId` 及本地修改版本跨 frame 对题；切题清空旧缓存，观察器仅在脚本启用后采集。
- 测试(shujiajia): 覆盖平台注册、迁移、Options、Manifest、两阶段后端、日志导出、音频观察、整段画段、写入事件、快捷键、面板、暂存状态与提交保护；真实页面提交按钮继续禁止触发。
- 资料(shujiajia): 初始化数加加平台与泸州话脚本边界，建立分段标注页外层、iframe 编辑器、稳定选择器及动态重渲染风险参考。
- 契约(shujiajia): 沉淀当前任务读取、annotation JSON 存储与暂存、最终提交链路及 `markResult.paragraphs[]` 核心字段职责；音频真实请求保持未确认，不根据展示字段或占位路径推断。
- 边界(shujiajia): 初始资料阶段未创建扩展运行时、后端或 Options；后续在本次整段识别接入中完成注册，合并远端发布提交后版本保持 `1.1.8`。

## 2026-08-30

- 修复(台州话内部质检): node 17 自动提交在分段“合格”切换后，不再使用 Arco 重渲染前缓存的行节点确认状态；现在固定按初始行键重新定位当前可见行，再确认语言种类与内部质检。行缺失、重复或不可见继续安全停止，失败面板仅显示分段序号、字段和确认阶段，不显示转写文本或行键。
- 测试(台州话内部质检): 覆盖连续三题中“直接提交 → 稳定行修正提交 → 单选后重渲染行修正提交”，以及重渲染后行消失时的分段级安全诊断。
- 修复(台州话内部质检): “保留 + 合格”题目的分段内部质检若“合格/不合格”均未选中，现在会在唯一、可见且可用的控件上自动选择“合格”，重新确认后再提交；重复选中、缺失或不可确认仍安全停止。
- 修复(台州话内部质检): node 17 `mark-package` 自动提交后，改以已锁定路由中的 URL `itemID` 变化确认进入下一题，不再依赖录音导入或 `Receive`、`Search`、`GetWorkItem` 快照；未确认时仍显示“提交可能已发送，未确认下一题”并安全停止。
- 修复(台州话内部质检): 为已授权的 node 17 自动提交增加扩展受限 `debugger` 鼠标输入。仅校验通过的当前顶层 AIDP 精确路由、`itemID` 和提交按钮中心可触发一次按下/抬起，随后立即断开；不读取 Cookie、网络请求、响应或页面数据，不调用 AIDP 提交接口，失败时不再回退 DOM `.click()`。
- 修复(台州话内部质检): 对需要从“未选中/不合格”改为“合格”的唯一可见分段单选，同样改用受限真实鼠标输入；后台动作白名单仅保留提交和该分段“合格”单选，控件未确认或输入失败仍立即停止。
- 修复(台州话内部质检): 受限真实鼠标输入补齐按下时的左键状态及抬起后的清空状态，形成平台可识别的完整单击序列；此前仅声明鼠标按钮名称可能导致第 4 段“合格”未实际选中。
- 优化(台州话内部质检): 对受限鼠标输入增加脱敏失败分类并透传至自动化状态面板，可区分无法附着标签页、按下/抬起未接受、断开失败和页面未确认；不再把所有分段修正失败合并为同一条提示。
- 修复(台州话内部质检): 网络安全等待改为在开始时记录页面已有未结算请求基线；内部页长期保持的背景请求不再导致永久等待，而本轮自动化新增的请求仍必须回落到基线并静默 1 秒才会继续。

## 2026-08-12

- 修复(release): ZIP 打包器不再清空 `dist/`，仅覆盖当前版本同名产物；恢复 `annotation-script-center-v1.1.5.zip`，确保历史版本包与 `1.1.6` 同时保留。
- 发布(1.1.6): 将已验收的台州话检查包、返修数据导入、审核结果人工回填、批量失败即停、真正新建成功后 1 秒间隔及后端确认复用后立即继续能力纳入正式版本；统一版本说明并按 ZIP-only 流程生成 `dist/annotation-script-center-v1.1.6.zip`。
- 优化(台州话返修): 批量添加经后端确认录音条目已复用时，继续计入“复用”并立即处理下一条，不重复创建也不执行新建成功后的 1 秒间隔；后端核验与失败即停边界保持不变。
- 优化(台州话返修): 列表批量添加在每条录音导入请求成功并完成安全映射保存后固定等待 1 秒再发起下一条，最后一条不额外等待；任意单条失败后立即停止整批并保留已有统计，不再请求后续 Item。
- 新增(台州话返修): 在 `/management/task-v2/{taskId}/node/14/revise?page={page}` 增加独立导入卡片，严格串行处理当前已加载页最多 10 条，支持开始、停止、single-flight、逐项统计及跨页/跨任务自动停止。
- 新增(台州话返修): 在 `/management/task-v2/{taskId}/modify-v2/4/{itemId}` 开放单题“添加数据”、结果刷新和人工“填入审核结果”；仅 `COMPLETED` 且 ItemID 精确一致时，向唯一可写返修 textarea 末尾换行追加，并阻止重复追加。
- 安全(台州话返修): MAIN-world 精确关联 `SearchModifyItem` 请求范围与响应，只发布 TaskID、页码、方向、捕获时间和每题允许字段；不保留人员信息、审核字段、请求头、Token、完整请求 URL 或其他业务字段，媒体 URL 只在当前页面内存使用。
- 边界(台州话返修): 返修页不自动暂存、审核、提交、切题或押后，不点击平台业务按钮，也不直接调用 AIDP 写接口；详情回填仅派发正常 DOM 输入事件，后续由用户人工检查并使用平台原生操作。
- 测试(台州话返修): 覆盖 Fetch/XHR 捕获、路由与快照校验、当前页串行导入、停止/跨页停止、任务编号禁用、重复映射复用、结果状态与 ItemID 校验、textarea 歧义/禁用/只读拒绝、换行追加和重复幂等。

## 2026-08-11

- 新增(台州话): `scan-v3/14/{itemId}` 与 `mark-package/{packageId}/14?itemID={itemId}` 两类检查包在保持 `readOnly=true` 的前提下，开放受控“添加数据”、录音结果刷新及用户显式启动的“全自动导入并押后”；`mark-v3` 既有顶部“添加数据”入口保持不变。
- 加固(台州话): 录音导入优先使用同题、未过期的 Search Item；检查包缺失时仅回退同题、未过期的 GetWorkItem/Receive，并只提取 `asr_text`、`audio`、`video`。GetWorkItem 回放保留原始 `capturedAt`，缺失、过期、错题或空内容均 fail-closed。
- 自动化(台州话): 当前题 ID 改由页面路由及匹配快照动态确认，作用域固定为页面类型、taskId 与 packageId/nodeId；跨包、切换页面类型、手动停止、DOM 歧义、非允许状态或下一题未验证时立即停止。每次点击前等待网络全部结算并静默 1 秒，20 秒超时停止，只操作唯一原生“押后”与原因 `1` 弹层。
- 安全(台州话): 检查包除录音导入和用户显式启动的原生押后外仍禁止文本写回、分段修改、暂存、保存、提交、领取及扩展直接切题；扩展不直接调用 AIDP 写接口。未配置录音任务编号时，`mark-v3` 隐藏顶部入口，检查包面板保留禁用入口并提示前往 Options 配置。
- 文档(台州话): 更正检查包当前能力的路由表述为 `/management/task-v2/{taskId}/scan-v3/{nodeId}/{itemId}` 与 `/management/task-v2/{taskId}/mark-package/{packageId}/{nodeId}?itemID={itemId}`；`nodeId` 仅白名单 `14、17`，不表示任意节点开放。检查包继续保持 `readOnly=true`，仅录音导入及用户显式启动的原生押后为既有例外。

## 2026-08-06

- 发布(1.1.5): 将扩展版本提升至 `1.1.5`，重新构建 Options 页面并生成唯一 ZIP 产物 `dist/annotation-script-center-v1.1.5.zip`；未执行服务器上传或公开下载中心部署。
- 安全(台州话): 检查包 `Receive` 跨页面世界快照收紧为当前题 `ItemID`、`Item.Content` 与 `TempAnswer.Content`；不再传递请求 URL、包元数据、审核历史或用户资料。
- 优化(台州话): 检查包只读批量识别改为默认不选分段，仅处理用户人工选择的当前可见分段，`check_status` 不触发自动选择；可写标注页保持原有默认全选与暂存行为。
- 测试(台州话): 覆盖 Receive 的 Fetch/XHR 最小快照、`Data.Items` 外层兼容，以及只读批量选择的默认空状态和人工点选。
- 安全(台州话): 检查包只消费带 `snapshotVersion: 1` 的最小 `Receive` 快照，并按路由题号与行键精确匹配；旧观察器消息、多题响应错题、缺失行键或无匹配行均 fail-closed。
- 安全(台州话): 检查包的删除当前选区、清空、分段预览与应用等写入型快捷键均拒绝执行，保留播放类快捷键。
- 测试(台州话): 覆盖旧版 Receive 拒绝、多题精确选题、行键失配空结果、只读快捷键无写入，以及 `data.items` 最小快照容器。

## 2026-08-03

- 修复(taizhou-recording): 新增录音结果只读恢复接口；手动刷新遇到浏览器本地映射缺失时，按任务编号、AIDP ItemID 与参考内容指纹恢复服务器已有映射并返回最新结果，全程不创建录音条目、不轮换幂等操作键。
- 修复(taizhou-recording): 扩展保存恢复接口重新签发的安全同步凭证；服务器无映射时保持正常空状态，远端条目不存在时明确提示点击“添加数据”重建，页面进入和周期性同步继续保持零恢复请求。
- 测试(taizhou-recording): 覆盖只读恢复成功、服务器无映射、参考指纹冲突、本地映射重建、自动进入零请求及安全错误提示，并同步收口扩展、平台、后端与接入文档。

## 2026-07-31

- 修复(taizhou): 检查包实际网络链路只发起 `Receive` 而未发起 `GetWorkItem` 时，只读识别现在回退复用已观察到的同页 Receive 最小快照；继续优先 GetWorkItem，不接入 SearchPackageItem，不增加任何 AIDP 写操作。
- 测试(taizhou): 新增检查包缺少 GetWorkItem、仅有 Receive 快照时仍可取得媒体与可见 regions 的回归用例。

## 2026-07-30

- 打包候选(1.1.4): 将台州话检查包只读识别改动生成本地验收 ZIP；尚未创建发布提交、tag、远端产物或部署。
- 修复(taizhou): 根据本地检查包验收路径，兼容 AIDP `mark-package/{packageId}/14?itemID={itemId}`；此前仅匹配 `scan-v3/14/{itemId}`，导致脚本虽启用但不会挂载面板或行内识别入口。
- 修复(taizhou): 主世界在当前页面内存缓存脱敏后的最近 `GetWorkItem` 快照，并仅响应检查包运行时的同源回放请求，避免异步设置读取导致错过初始化音频与 regions；不持久化、不写后端、不写 AIDP。
- 修复(taizhou): 将具备 `GetWorkItem` 捕获能力的主世界观察器安装标记与旧版通用标记拆分；本地同时启用旧版扩展时，1.1.4 仍可安装新版观察器并读取检查包媒体快照。
- 测试(taizhou): 覆盖旧版观察器标记已存在时，新版仍捕获并最小化 `GetWorkItem` 响应的回归。
- 新增(taizhou): 台州话支持 AIDP `scan-v3/14` 检查包只读识别。当前题从最小 `GetWorkItem` 快照读取媒体与分段，按 Arco 行键关联 `region.id`，支持单段/当前可见分段批量识别、停止、预览与复制。
- 安全(taizhou): 检查包模式不显示或执行写入型操作；数据层对分段应用、清空、填语言、单段填入和批量写回均 fail-closed，不修改 textarea、不调用暂存、提交、领取、切题、录音导入或押后流程。
- 测试(taizhou): 覆盖检查包路由、GetWorkItem fetch 快照最小化、regions 关联、批量只读预览、停止以及无写请求回归。

本总账只记录 DataBaker CVPC 柳州话、ByteDance AIDP 苏州话、金华话与台州话、Magic Data 杭州话、数加加泸州话，以及六脚本共同依赖的扩展、Options、后端、测试和发布能力。

## 2026-07-27

- 修复(taizhou-recording): 移除“添加数据”点击入口对浏览器本地旧映射的页面级提前返回；本地已有映射时仍进入脚本中心后端核验，失效条目可按既有 `TASK_ITEM_NOT_FOUND` 流程重新创建，有效条目则在服务端确认后提示并刷新。
- 测试(taizhou-recording): 将旧映射零请求断言改为页面级权威核验回归，覆盖失效映射返回新条目、有效映射服务端重放、无 Toast 回退状态与结果刷新。
- 修复(taizhou-recording): 用户主动点击“添加数据”时不再被浏览器本地旧映射短路；脚本中心先核验录音平台条目，明确收到 `TASK_ITEM_NOT_FOUND` 后轮换持久化幂等操作键并重新创建，其他查询故障保留旧映射且不创建。
- 修复(taizhou-recording): 结果查询遇到已删除录音条目时显示“原录音条目已不存在，请点击添加数据重新创建”；页面进入、自动查询和手动刷新继续保持只读。
- 测试(taizhou-recording): 覆盖有效旧映射核验重放、缺失条目换键重建、401/429/503 保留映射、本地映射下人工请求交由后端判定，以及缺失条目查询不触发创建。
- 修复(taizhou-recording): 本地同步映射不再参与台州话运行时配置签名，保存映射不会重建 dataApi 或丢失当前 Search Item 快照；HTTP 201 创建和 HTTP 200 幂等恢复后立即自动查询一次录音结果，失败时保留映射与创建响应状态供手动刷新。
- 优化(taizhou-recording): 已导入条目的原生确认框改为页面顶部居中的蓝色信息图示 Toast，固定 1 秒自动消失、支持点击关闭并保持重复添加零创建、零结果请求和零结果重绘。
- 测试(taizhou-recording): 覆盖映射变化不触发运行时重建、真实配置变化仍生效、创建/重放后一次结果查询、自动刷新失败保留状态，以及 Toast 单实例、计时重置、点击关闭和销毁清理。
- 修复(taizhou-recording): “添加数据”在进入创建流程前及流程内部双重检查当前题目的本地同步映射；已导入条目只弹窗提示现有录音条目，不再改写已完成等结果状态，也不重建音频播放器。
- 修复(taizhou-recording): 未导入题目手动刷新改为本地正常空状态且不发请求；首次创建立即展示响应中的真实 itemCode 与 status，HTTP 200 幂等重放可重建丢失映射并提示条目已存在。
- 测试(taizhou-recording): 覆盖本地重复拦截零请求、空映射刷新零请求、创建状态透传、服务端重放恢复、连续点击与现有结果不重绘。
- 修复(taizhou-recording): Search Item 响应包含多条数据时不再固定读取第一条；MAIN-world 仅提取每条的 ItemID、参考文字及音视频 URL，隔离世界按当前 Receive ItemID 精确选择，保持错题、过期与空内容安全拒绝。
- 测试(taizhou-recording): 新增多条 Search Item 安全脱敏与当前题目位于非第一项的回归覆盖，确认不传递用户信息、完整响应或认证字段。
- 修复(taizhou-recording): 顶部改为通用“台州话脚本已就绪”状态；Receive/Search Item 等待、过期和错题等被动状态只控制“添加数据”按钮及悬停说明，不再覆盖导入、刷新或识别操作结果。
- 修复(taizhou-recording): 每次进入题目只读取映射并自动查询、渲染录音结果一次；同题目的周期性 DOM 同步不再重读映射、重渲染结果或重建音频播放器，面板重建改为从 UI 内存恢复，后续查询仅由手动刷新触发。
- 测试(taizhou-recording): 覆盖公共状态隔离、导入成功提示跨周期保留、单代次一次自动同步、A→B→A、面板重建结果恢复及音频元素稳定性。

## 2026-07-26

- 修复(taizhou-recording): AIDP 重绘辅助面板后恢复最近一次顶部文案；导入上下文只在题目或就绪原因变化时更新提示，等待、过期、错题和空内容继续严格禁用“添加数据”并给出具体原因。
- 修复(taizhou-recording): 同一题目同一进入代次缓存最近有效录音结果，周期性 DOM 同步不再发起重复自动查询，也不再以空状态覆盖“待领取”等有效状态；手动刷新和 A→B→A 新代次查询保持不变。
- 优化(taizhou-recording): 结果卡片移除“来源题目”，未导入统一显示“录音条目：还未导入该条目”，补充“已废弃”状态，未来未知状态继续显示原始状态码。

## 2026-07-25

- 实现(taizhou-recording): 台州话录音目标由数据库内部 taskId 改为可见任务编号；脚本中心通过任务编号端点创建条目，并固定绑定 `BYTEDANCE_AIDP + AIDP ItemID`，同一来源只在同一录音任务内唯一。
- 迁移(taizhou-recording): 扩展 storage schema 升至 36、后端状态升至 v3；旧内部 taskId 设置不自动迁移，旧 taskId-only 同步映射按确认规则丢弃，服务器允许列表改为 `allowedTaskCodes`。
- 测试(taizhou-recording): 跨层定向 Node 测试 100/100、Options 定向测试 34/34 与完整 `npm test` 通过；Options 生产构建、修改脚本语法检查、差异格式检查及真实示例窄范围泄漏扫描通过。
- 调整(taizhou-recording): 台州话“添加数据”改为直接传递 Search Item 的参考文字及公网 HTTPS 音视频 URL，不再下载或上传参考媒体；脚本中心移除参考媒体上传/公开托管路由，以 URL 指纹保持幂等与内容冲突保护，并将状态升级到 v2 安全清理固定旧媒体目录。录音成果的短时签名代理保持不变。
- 优化(extension): 首次安装与重置设置时五个脚本全部默认关闭，不再默认启用柳州话或选择苏州话；已有安装明确保存的启停状态继续保留。
- 优化(bytedance-aidp): 苏州、金华、台州统一将“当前音频信息”改为“当前媒体信息”，从 Receive 条目展示音频和视频地址，移除模板展示，缺少视频时显示“无视频”。
- 优化(taizhou-recording): 台州话录音导入按钮改名为“添加数据”并固定在“清空画段”左侧；既有 Search Item 校验、媒体下载托管与录音平台写入链路不变。
- 优化(taizhou-recording): “当前录音平台结果”改为与媒体信息、AI 信息一致的默认折叠区，继续只读展示状态、完成文本与结果音频。
- 修复(taizhou-recording): 签名媒体下载改为明确不携带浏览器凭证，避免媒体响应允许通配来源时因 `credentials: include` 被 CORS 拦截。
- 测试(extension): 覆盖五脚本首次安装默认关闭、AIDP 三脚本音视频上下文、模板隐藏、无视频提示、台州按钮顺序及录音结果折叠。
- 优化(bytedance-aidp): 苏州、金华、台州“当前媒体信息”为实际存在的音频与视频 URL 增加复制按钮，保留完整查询参数；缺少视频时不显示复制操作。
- 优化(taizhou-recording): 将“刷新录音结果”移动到来源题目、录音条目和状态所在的响应式信息栏，空映射及每次结果重渲染后位置保持一致。
- 测试(extension): 覆盖三套媒体 URL 精确复制、失败提示、按钮文案恢复、无视频隐藏操作，以及台州各结果状态下刷新按钮布局。

## 2026-07-24

- 新增(taizhou-recording): 统一后端增加台州话受控媒体上传、长期参考媒体托管、录音条目幂等转发、完成结果查询、短时签名录音代理与公开媒体 Range 读取。
- 新增(taizhou-recording): 台州话 Options 基础设置增加录音平台数据库内部 taskId；详情页观察 Search Item 最小字段，人工导入当前完整题目，并在辅助面板只读回显任务状态、完成文本与音频。
- 安全(taizhou-recording): storage schema 升至 35，仅保留最近 500 条安全同步映射；Options taskId 与服务器 allowedTaskIds 双重匹配，无需管理员解锁，扩展不保存机器 Key、登录态、参考全文或原始签名媒体 URL。
- 测试(taizhou-recording): 覆盖 Search Item fetch/XHR 严格脱敏、ItemID 一致性与过期、文本/音频/视频组合、100MB 双重限制、媒体部分失败、重复导入、单次自动查询、手动刷新、状态映射和结果不写回 AIDP。
- 安全(taizhou-recording): 录音平台机器 Key、任务允许列表与签名密钥只从 Git 忽略的服务器私密 JSON 读取；状态不保存参考全文、同步 token 明文、AIDP 登录态或本地绝对路径。
- 测试(taizhou-recording): 覆盖配置、任务允许列表、严格字段、流式 100MB 上限、MIME/魔数、并发、原子落盘、TTL、稳定幂等、恢复、4xx/5xx清理策略、同步凭证、音频代理及公开媒体 Range。
- 加固(taizhou-recording): 复核读取授权、上传超限与慢流、同源单飞、启动和定时恢复、音频流全生命周期取消、请求指纹及上游标识脱敏，任务移出允许列表后旧同步和播放凭证立即失效。
- 加固(taizhou-recording): 活动 staging 跨维护周期受保护，上传准备异常必定释放并发槽位；创建和结果 JSON 超时覆盖完整 body 并限制 256KB，公开媒体 416 保留 CORS 且安全处理文件流错误。
- 文档(integration): 新增录音任务平台长期接入规范，固定专用机器 API、三类参考字段、幂等重试、错误处理和脱敏调用示例。
- 安全(integration): 明确录音平台 API Key 仅存服务器私密配置；受登录态保护的媒体由浏览器取得字节，不向服务器传第三方 Cookie、Authorization 或 Session。
- 修复(taizhou-recording): 请求指纹改用 trim 后文字与媒体内容 SHA-256，同字节重传可恢复映射并清理重复上传，不同内容保持 409 冲突。
- 修复(taizhou-recording): 浏览器流式累计媒体实际字节并在超过 100MB 时取消；确定性 4xx 丢弃 pending，网络、5xx 与可重试状态保留原请求。
- 修复(taizhou-recording): 结果查询按题目进入代次隔离慢响应，A→B→A 重新查询；结果音频只接受脚本中心预期相对 API 路径。
- 修复(taizhou-recording): 题目代次在映射查询前登记并集中 guard，旧题慢映射、结果和错误均不得覆盖新题或切回旧代次。
- 修复(taizhou-recording): 导入固定使用启动时的 taskId、sourceItemId 与 pending key；切题后只保存原题映射，不在新题回显原题状态。
- 安全(taizhou-recording): 结果音频 token 严格限定为预期相对路径下的两段非空 base64url，拒绝绝对 URL、额外段、查询和片段。
- 修复(taizhou-recording): 成功映射的同步凭证改由服务器密钥与映射键稳定派生，跨浏览器或重装后的幂等重放不再覆盖其他客户端凭证。
- 安全(taizhou-recording): 运行目录已有数据但状态文件缺失、损坏或结构无效时返回脱敏 503，保留长期媒体并停止孤儿清理；录音 taskId 默认值仅归属台州话脚本。
- 边界(integration): 当前运行时接入仅限 ByteDance AIDP 台州话完整题目人工导入与只读结果；其他平台仍需逐一显式确认导入粒度与字段来源。

## 2026-07-18

- 发布(1.1.1): 发布双 DashScope 密钥切换兼容版本；五个语言助手已完成服务器双槽位人工验收，切换只影响后续 AI 请求，密钥鉴权失败不自动回退或切换另一把密钥。
- 修复(backend): 双 DashScope 密钥解析增加旧单密钥兼容边界：仅双槽位均未配置时回退旧变量；任一槽位存在后严格使用当前槽位，空槽位不发起上游请求。
- 修复(ai): 柳州、苏州、金华、台州共用 Qwen / Qwen Python / Fun-ASR REST/Python 与杭州话专用 Qwen 客户端统一携带当前槽位来源；百炼 401 只提示当前密钥来源鉴权失败，不自动改用另一账户。
- 优化(options): 系统管理页明确显示“旧密钥兼容中”与“未配置可用密钥”，不展示密钥、路径或指纹。
- 测试(backend): 覆盖旧变量回退、槽位优先、五语言 Provider 动态切换、401 不重试及管理员安全状态响应；Options 覆盖兼容和未配置状态。
- 文档(config): 更新双密钥迁移、旧变量回退优先级与 PM2 配置说明。

## 2026-07-17

- 发布(zip): 重新生成 `annotation-script-center-v1.1.0.zip`，固化当前五脚本、服务器双 DashScope 密钥切换与 Options 密钥控件；对应 Git tag 为 `v1.1.0`。
- 新增(backend): 增加服务器双 DashScope 密钥槽位，固定读取两份私有密钥文件并以私有状态文件持久化当前槽位；Qwen、Fun-ASR 与杭州话统一使用当前选中密钥，不自动故障切换。
- 新增(options): 系统管理的服务器后端设置新增双密钥安全控制，复用管理员会话且不展示密钥、文件路径或指纹。
- 优化(options): 双密钥控制收紧为“吴 / 王”按钮加“保存当前密钥”；选择按钮不发请求，保存后才切换且刷新状态，401 退出会话，404 只提示接口尚未部署。
- 优化(options): 将服务器 AI 密钥紧凑控制器移入后端根地址卡，固定显示在当前生效地址下方、根地址配置操作上方；吴 / 王复用服务器 / 本机的分段切换外框与选中样式，窄屏自动换行，避免展开地址配置时控制器落出可视区。
- 测试(backend): 覆盖默认槽位、持久化切换、非法或未配置槽位、状态损坏、管理员鉴权和安全响应边界。
- 文档(config): 明确双密钥私有文件、旧单密钥迁移、PM2 文件权限和手动服务器验收步骤。
- 优化(ai): storage schema 升至 `34`，柳州听音/普通话整理与杭州听音/普通话整理/单模型分别提供默认开启的字词表开关、字词表提示词、Prompt 和生成参数。
- 重构(magic-data): 杭州话移除识别策略 UI 与代码侧普通话转方言、最终用字归一化路径；双模型按真实读音听写方言后再按语义整理普通话，单模型改为一次调用同时输出两类文本。
- 优化(lexicon): 柳州、杭州只把 JSON 主词表中最多 `30` 条相关词条写入启用阶段 Prompt；关闭、缺失、非法或无命中时按无词表模式继续，参考 CSV/XLSX 不参与运行时结果。
- 兼容(api): 新请求优先使用 `aiStages.listen/refine/single` 与 `defaults.stages`，旧扁平字段保留一个迁移周期；响应增加阶段 `enabled/contextEntryCount`，旧匹配/转换字段保持空值并弃用。
- 测试(ai): 补齐 schema 迁移、Options 字段显隐、阶段请求归一化、词表开关与相关词筛选、杭州 Prompt 和不再代码改写等回归覆盖。

## 2026-07-14

- 重构(bytedance-aidp): 台州话切换为原始听音直填诊断模式。后端仅接受严格 JSON 的 `listenText`，移除普通话转换、文本清洗、风险/复核、强制填入和自动填入分支；单段直填 textarea，批量仅暂存写入当前页 `regions[*].txt`，全程不保存、不提交、不切题。
- 测试(bytedance-aidp): 覆盖 `listenText` 原样保留、空/非法 JSON 不写入、单段直填、批量单次暂存写回、停止批量不二次写回，以及台州 Options 不显示自动填入开关。
- 重构(bytedance-aidp): 台州话识别改为每段单次 Qwen Omni 调用，默认 `qwen3.5-omni-plus`，仅允许切换至 `qwen3.5-omni-flash`；听音、普通话转换、风险判断和复核信息合并为同一 JSON。
- 优化(bytedance-aidp): Storage schema 升至 32；仅把旧台州听音模型迁移到全模态模型，旧两阶段 Prompt 与生成参数不复用。前端请求、AI 面板和 CSV 日志统一为 `omni` 模型、usage、cost、raw 与 debug 字段。
- 修复(bytedance-aidp): 最终普通话文本增加代码级标点白名单、数字汉字化、无效字符移除及明显口吃式单字/短词重复压缩；JSON 解析失败时强制人工复核并阻止自动填入。
- 测试(bytedance-aidp): 覆盖单次调用、Plus/Flash 白名单、60 秒上限、JSON 失败保护、风险阻断、文本清洗、schema 32 迁移、单 Omni 请求体、Options 映射和面板展示。

## 2026-07-13

- 新增(bytedance-aidp): 接入台州话独立 runtime、设置、后端路由、AI 调用日志和平台资料。
- 优化(bytedance-aidp): 将苏州话、金华话、台州话升级为同平台三方互斥，storage schema 升至 31 且台州默认关闭。
- 测试(bytedance-aidp): 覆盖台州话风险拦截、强制填入、批量待复核、三方互斥、Options、manifest、路由和日志数据集。
- 修复(bytedance-aidp): 停止台州话批量识别后，已在途结果改为保留待用户显式填入，不再自动暂存写回。
- 修复(bytedance-aidp): 台州话单段与行内自动填入在异步返回前校验题目/分段快照和输入框空值；拦截结果保留给用户显式填入。
- 修复(bytedance-aidp): 台州话风险布尔值仅接受原生 `true` 或去空格、忽略大小写的字符串 `"true"`，refine 标点收口为 `，。？！`；补齐 schema 30 升至 31 的三方互斥迁移边界，以及 DOM 直填与 `SubmitTempItemAnswer` 暂存写回的资料契约。

## 2026-07-15

- 重构(jinhua): 金华话从两阶段普通话翻译切换为单次 Qwen Omni 原始听音直填；业务结果只保留逐字符原样的 `listenText`。
- 重构(jinhua): 移除风险、待填入、强制填入与自动填入运行路径；批量只更新当前页选中段的 `regions[*].txt`，不修改 `ms`、不提交、不切题。
- 配置(jinhua): storage schema 升级至 `33`，旧两阶段配置仅作为非运行时兼容数据保留；Options 只展示原始听音 Omni 设置与调用诊断信息。
- 测试(jinhua): 覆盖单次 Omni 白名单与严格 JSON、逐字符直填、零选中批量、停止无二次写回及旧配置隔离。
- 修复(bytedance-aidp): 金华话与台州话的 Qwen Omni 改为直接输出最终转写纯文本；后端仅把字符串原始输出逐字符映射到扩展/API 兼容字段 `listenText`，不再 JSON 解析、trim、清洗、提取或猜测。所有非空字符串（包括意外的 JSON、Markdown、解释）均原样传递；空字符串或非字符串不写入，`raw.omni` 继续用于诊断，保存的自定义 Prompt 不迁移、不覆盖。
- 测试(bytedance-aidp): 覆盖金华话、台州话的纯文本与含空白输出逐字符映射、意外 JSON/Markdown/解释原样传递、空/非字符串不写入，以及默认和实际请求 Prompt 不再要求 JSON；保持 60 秒超时、模型白名单、thinking、usage/cost 回归覆盖。
- 修复(bytedance-aidp): 金华话与台州话的非空自定义 Prompt 改为原样作为完整 systemPrompt；后端只附带音频片段、时间范围和标注上下文，不再追加不翻译、原样听写、纯文本或其他输出格式规则。清空自定义 Prompt 后仍回退各自默认 Prompt，模型原始字符串继续逐字符映射到 `listenText`。
- 测试(bytedance-aidp): 覆盖自定义 Prompt 完整透传、用户消息仅含上下文且不含后端追加规则，并回归验证默认 Prompt 回退、原样响应映射、thinking、超时、模型白名单与费用统计。

## 2026-06-12

- 修复(data-baker-cvpc): 将“未填写补 Valid”改为平台直写，并保留失败提示与页面状态同步。
- 优化(data-baker-cvpc): 收口柳州话最终文本标准写法、普通话顺滑、近音纠错与标签识别。
- 修复(data-baker-cvpc): 单独语气词统一应用 Meaningless 预设，避免带标签填入失败时清空标注文本。
- 优化(data-baker-cvpc): 接入识别后自动填入、AI 价格估算、原始返回复制和中文日志表头。
- 优化(data-baker-cvpc): 完成画段建议、静音阈值、前后补偿、整音频预览与页面内应用。
- 新增(data-baker-cvpc): 落地柳州话运行时主词表、两阶段 AI 设置、批量识别和字段填入快捷键。
- 修复(data-baker-cvpc): 补齐 iframe 音频捕获、鉴权回退、早期挂载保护和提示屏蔽开关。
- 文档(data-baker-cvpc): 同步柳州话运行时、后端与平台参考资料。

## 2026-07-01

- 文档(bytedance-aidp): 初始化苏州话平台入口、页面结构与 Network 稳定参考。
- 新增(bytedance-aidp): 接入苏州话最小运行时、脚本启停、平台 AI 显隐和当前音频信息。
- 新增(bytedance-aidp): 建立苏州话详情页基础设置、AI 设置与快捷键入口。

## 2026-07-02

- 新增(bytedance-aidp): 接入苏州话分段建议、画段后自动应用、暂存直写和语言种类填充。
- 优化(bytedance-aidp): 补齐播放倍速、波形缩放、前后静音、静音阈值和连续段合并配置。
- 修复(bytedance-aidp): 收口平台 AI 显隐漂移、清空画段条件和默认 Space 快捷键迁移。
- 测试(bytedance-aidp): 补充分段控制、设置归一化、暂存直写与快捷键回归测试。

## 2026-07-03

- 新增(bytedance-aidp): 完成苏州话单段 AI 识别、批量识别、调用记录和结果写回。
- 优化(bytedance-aidp): 收口普通话听写、方言翻译、数字与标签格式规则。
- 修复(bytedance-aidp): 解决语言种类批量填充卡住和虚拟表格行定位问题。
- 文档(bytedance-aidp): 同步苏州话 Options 字段、Prompt 规则和写回边界。

## 2026-07-04

- 优化(bytedance-aidp): 统一苏州话前端样式、行内识别、AI 信息区与快捷键交互。
- 修复(bytedance-aidp): 修复设置页 AI 面板与快捷键空白，并统一脚本启停状态。
- 修复(bytedance-aidp): 单段普通话听写稿写回后主动失焦，避免焦点滞留输入框。

## 2026-07-06

- 新增(bytedance-aidp): 接入金华话运行时、后端 AI 推荐、批量识别和设置详情页。
- 优化(bytedance-aidp): 苏州话与金华话使用同平台互斥模型，启用一个脚本时自动关闭另一个。
- 新增(bytedance-aidp): 管理区增加切换账号入口，并通过扩展权限清理 AIDP 登录状态。
- 测试(bytedance-aidp): 补齐双脚本互斥、账号切换、设置保存和运行时注入测试。

## 2026-07-07

- 优化(bytedance-aidp): 统一金华话与苏州话设置页双栏布局、问号帮助、下拉框和多行输入交互。
- 修复(bytedance-aidp): 金华话独立读写 `jinhuaHelper`，不再读取苏州话配置键。
- 修复(bytedance-aidp): 为平台 AI 隐藏节点增加脚本 owner 标记，避免双脚本互相恢复。
- 修复(bytedance-aidp): 恢复金华话当前音频、单段识别、批量识别和分段建议的数据上下文。
- 优化(bytedance-aidp): 播放期间冻结助手重挂、上下文刷新和表格补挂，停止推进后再恢复。
- 修复(bytedance-aidp): 平台 AI 隐藏不再误伤题目头部暂存与重置操作区。

## 2026-07-08

- 新增(magic-data): 接入杭州话运行时、后端 AI 质检、共享采集和结果面板。
- 调整(magic-data): 杭州话说话人属性增加“音频是否是纯方言”判断，并接入 AI 输出与前端展示。
- 测试(magic-data): 补齐杭州话 storage、Options、popup、manifest、前端面板和后端路由回归测试。
- 优化(bytedance-aidp): 金华话听音阶段负责粗听文本与唱歌/非金华话判断，普通话阶段负责格式收口与自动填入决策。
- 新增(bytedance-aidp): 金华话 AI 推荐增加风险字段、强制填入当前段和批量待复核统计。
- 测试(bytedance-aidp): 补齐金华话后端、行内识别、风险拦截和强制填入测试。

## 2026-07-09

- 新增(magic-data): 接入杭州话 JSON 主词表、结构校验、运行时加载和无词表降级。
- 优化(options): 四脚本详情页统一使用共享下拉、快捷键面板和两栏设置工作台。
- 修复(options): 自定义下拉改为页面顶层浮层，并处理菜单滚动、页面滚动和响应式关闭策略。
- 优化(options): 功能面板恢复平台顺序编辑，系统管理收口后端、下载与 AI 日志入口。

## 2026-07-11

- 发布(1.0.0): 柳州话、苏州话、金华话和杭州话组成唯一公开脚本集合。
- 收口(extension): manifest、popup 和 Options 只保留三个平台四个脚本的运行时入口。
- 收口(backend): 后端只注册四脚本路由、管理员会话、下载中心和四脚本 AI 日志。
- 发布(build): manifest 版本为 `1.0.0`，发布流程固定生成版本化 CRX/ZIP 与两份更新元数据。
- 优化(options): 四脚本详情页恢复运行时完整设置，统一问号帮助、字段顺序、双栏布局和响应式控件。
- 优化(options): 平台地址按域名与路径拆行，侧栏、返回按钮和平台地址胶囊不显示文字下划线。
- 新增(options): 接入四脚本 defaults 接口和本地回退，保护未保存草稿并清理等于后端默认值的覆盖项。
- 修复(storage): 保留 AIDP 两阶段生成参数，支持 CVPC `db / ratio / value` 静音单位，统一杭州模型与识别枚举。
- 修复(options): 非法数字阻止整次保存，保存后重新读取归一化设置，确保刷新前后状态一致。
- 重构(options): 全局样式迁移为模块化 SCSS，按 foundation、components、layouts、pages 和 vendor 分层。
- 重构(test): 全部长期测试集中到根 `tests/`，并新增统一 `npm test` 与五个定向分区命令。
- 修复(test): 测试临时数据改用系统临时目录，Options 缓存固定写入忽略目录并自动清理。

## 2026-07-12

- 配置(domain): 默认 Server、扩展更新地址、发布下载基址和后端下载中心统一使用 `annotation-script-center.xiangtianzhen.store`。
- 清理(project): 删除非四脚本资料与过程入口，当前文件树只描述四脚本正式版本。
- 清理(storage): schema 固定为 30，不读取或迁移其他版本的后端地址。
- 文档(log): 重建修改总账，只保留四脚本及其共享基础设施记录。
- 文档(readme): 恢复项目首页、扩展、配置、平台、统一后端、Magic Data 与公共 AI 模块的完整使用说明。
- 发布(zip): 1.0.0 私有分发永久收口为单一版本化 ZIP，删除签名、自动更新元数据与旧发布配置。
- 优化(download): 下载中心只读取服务器 ZIP 目录索引，并说明 Chrome / Edge 开发者模式手动加载流程。
- 安全(config): 本地后端鉴权配置收口为管理员密码哈希与随机 JWT 密钥，私有发布文件不再参与项目流程。
- 文档(deploy): 补充 Windows 与 Linux 统一 Python 虚拟环境的创建、依赖安装、服务器更新和 CVPC 画段排障流程。

## 2026-07-15

- 修复(release): Windows ZIP 打包改为逐文件写入标准 `/` 路径，避免 Edge 拖拽导入时无法按 manifest 找到脚本。
- 测试(release): 新增实际归档回归测试，校验 ZIP 不含反斜杠路径且完整包含 manifest 引用脚本；支持临时源目录与输出目录隔离打包。
- 文档(release): 明确 ZIP 路径兼容性校验，并保留解压后加载作为标准安装方式。
- 优化(jinhua): 后端内置金华话转写 Prompt，支持使用者保存本地自定义 Prompt 完整覆盖默认主体，并保留 JSON `listenText` 传输契约。
- 测试(jinhua): 覆盖默认 Prompt、用户 Prompt 原样透传、空白覆盖回退和扩展请求序列化。
- 文档(jinhua): 同步金华话转写 Prompt、JSON 响应与本地覆盖边界。
- 优化(bytedance-aidp): 金华话与台州话的单次 Qwen Omni 设置新增可持久化的思考开关，默认关闭；开启时仅以严格布尔 `aiOmni.enableThinking: true` 透传，苏州话保持固定关闭，业务响应仍只展示 `listenText`。
- 测试(bytedance-aidp): 覆盖思考开关的 Options 显示与保存、扩展请求体、后端严格布尔解析及 Qwen 调用参数。
- 修复(bytedance-aidp): 公共 Qwen provider 不再忽略金华话、台州话显式开启的思考开关，现将其真实发送为百炼 `enable_thinking: true`；默认及非 `true` 值仍关闭，并保留参数不支持时移除后重试。
- 测试(bytedance-aidp): 覆盖 Qwen 最终请求体在显式开启时包含 `enable_thinking: true`。
- 发布(1.1.0): 金华话与台州话提升 Omni 输出文本质量：模型原始字符串原样映射至 `listenText`，非空自定义 Prompt 不再被后端追加规则覆盖，使用者可准确控制普通话转换与输出格式；本地 Edge 验收通过。

## 2026-07-28

- 修复(台州话): 用户明确授权录音平台 `SUBMITTED`（待审核领取）与 `AVAILABLE`（待领取）同为自动押后可继续状态；其他状态仍停止。失败面板现在显示收到的原始状态码，便于排查状态差异。
- 测试(台州话): 覆盖 `SUBMITTED` 的原生押后完整链路、零“提交”点击、非可押后状态的原始状态码提示，以及面板“等待可押后”状态文案。
- 新增(台州话): 在详情页辅助面板增加用户显式启动的“全自动导入并押后”。每轮复用录音导入与结果刷新，只在 `AVAILABLE`（待领取）或 `SUBMITTED`（待审核领取）后通过真实 DOM 操作“押后”并填写原因 `1`，以 Receive ItemID 切换确认进入下一题。
- 安全(台州话): 自动流程只匹配精确的“押后”和唯一“押后原因”弹层，不点击“提交”、领取或 AIDP 接口；任何 DOM 歧义、状态不符、写入失败、20 秒超时或未验证下一题均停止，确认前停止会取消弹层。
- 测试(台州话): 覆盖完整成功链路、已有映射刷新、非待领取、缺失/多重弹层、原因写入失败、超时、下一题未验证、用户停止、无可押后数据及面板状态渲染。
- 修复(台州话): 导入结果返回后，首轮也最多等待 20 秒供页面右上角“押后”按钮完成重渲染；按钮延迟出现时继续执行，超时后才报告无可押后数据。
- 修复(台州话): 已由真实详情页 DOM 确认右上角“押后”为类名含 `defer-button` 的 `div`，不是原生 `button`；自动流程在保留原生 `button` 支持的同时，仅对白名单内的该 `div` 控件执行点击，普通同名元素和“提交”仍不会命中。
- 修复(台州话): 可见性遍历现在在真实浏览器 `Document`（`nodeType=9`）节点停止，避免把整页误判为不可见；回归用例使用无 `tagName` 的浏览器文档形态验证顶部 defer 控件仍可被点击。
- 修复(台州话): Receive ItemID 切换到下一题后，自动流程先在 20 秒内只读等待同题 Search Item 完整快照；`waiting`/`stale` 不再被当作导入失败，快照就绪前不会发送录音导入请求，其他上下文异常仍立即停止。
- 发布(1.1.2): 完成台州话全自动导入并押后修复的真实浏览器验收，版本升级为 1.1.2，并按 ZIP-only 流程重新构建扩展安装包。

## 2026-07-29

- 修复(台州话): 全自动导入并押后在每次自动点击前，等待 AIDP 页面 fetch/XHR 全部结算并连续静默 1 秒；20 秒内未满足时显示未结算数量并安全停止，用户停止前的取消弹层保持立即执行。
- 安全(bytedance-aidp): 主世界网络活动观察只向扩展传递未结算数量和事件序号，不传 URL、请求参数、响应、Cookie 或授权信息。
- 测试(台州话): 覆盖 fetch/XHR 生命周期、同源网络快照、结算后的静默窗口重置、未结算超时停止、等待阶段面板状态与零“提交”点击。
- 发布(1.1.3): 发布台州话自动点击网络结算安全闸门，升级扩展当前版本并按 ZIP-only 流程重新构建唯一安装包。

## 2026-08-30

- 新增(台州话): 仅在 `mark-package/{packageId}/17?itemID={itemId}` 内部质检包增加用户手动启动的“全自动质检并提交”；普通标注页、其他检查包和既有“全自动导入并押后”互不影响。
- 安全(台州话): 丢弃或整体不合格只通过唯一真实右上角“提交”按钮提交；保留且合格时逐段确认“目标方言 + 内部质检合格”后提交。所有 DOM 歧义、字段未确认、网络等待超时、范围变化、停止操作或下一题未确认都会立即停止，不直接调用 AIDP 提交接口。
- 测试(台州话): 增加 node 17 路由限制、直接/修正提交分支、真实提交控件、分段逐条修正、网络未结算、提交后未确认下一题及独立面板状态的回归覆盖。
