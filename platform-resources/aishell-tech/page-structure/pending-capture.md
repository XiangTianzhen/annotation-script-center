# 待补采项

## 页面结构

### 视图/角色级

2. **质检/验收员视图**：`.check-area` 可见时的完整 DOM、按钮组和交互行为
3. **我的团队**（`/organization/myteam`）：完整 DOM 采集

### 弹窗/对话框级

4. **历史标注记录弹窗**（`.el-dialog__title`="标注历史记录"）：弹窗 DOM 和内容渲染
5. **修改任务信息**（`.el-dialog__title`="修改任务信息"）：弹窗 DOM
6. **数据分包**（`.el-dialog__title`="数据分包"）：弹窗 DOM
7. **定向分配**（`.el-dialog__title`="定向分配"）：弹窗 DOM
8. **选择团队**（`.el-dialog__title`="选择团队"）：弹窗 DOM
9. **完成质检包**（`.el-dialog__title`="完成质检包"）：弹窗 DOM
