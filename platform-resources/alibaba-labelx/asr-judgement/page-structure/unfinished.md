# ASR 快判未完成事项

## 页面降载

- `100/150/200 条/页` 自定义大页数暂不开放；`400 条/页` 已恢复为全量显示测试入口。
- LabelX 页面在大页数下仍可能有显示和交互稳定性问题，后续需要重新设计窗口化或其他降载方案。
- `judgement-virtual-window.js` 保留实验代码，但当前 options 不展示开关，运行时强制关闭。
- 已尝试按当前题号只展开前后 5 题，并对窗口外题卡写入隐藏标记和 inline CSS 变量，实测未能稳定压缩宿主页面题卡。

## 轻量题卡摘要

- 摘要内直接选择按钮暂不实现。
- 如果后续要在摘要块内提供直接选择按钮，必须先验证 Ant Design Radio 受控状态、保存请求和答案回显是否可靠触发。

## 网络采集

以下路径仍需补充采集：

- 自动领取成功进入新详情页。
- 完成全部题目后的服务端提交失败响应。
- 筛选“回答区数据”的 `filter.questions` 结构。
- 音频加载失败、保存失败等异常路径。

## 全自动能力边界

- 自动选择、自动保存、自动提交、自动领取和自动流转属于全自动能力。
- 进入全自动前必须补齐保存、提交、失败响应、校验阻断、自动领取成功 / 失败等网络采集。
- 未验证前，不允许让脚本静默提交或批量改写标注结果。
