# 下次接续采集说明

## 当前上下文

本轮已完成并写入文档的内容包括：

- 首页手动领取、连续领取、释放。
- 首页未完成包点击 `标注` 打开新详情页标签页。
- 详情页初始化、数据读取、统计、面板、会话、模板读取。
- 单选保存、填空保存、保存边界。
- 每页条数、真实翻页、任务状态筛选。
- 音频播放、暂停、拖动、快进、后退、倍速、重载。
- 题卡鼠标选中和快捷键观察。
- 未完成包点击提交时的前端必填校验阻断。
- 提交成功响应体：`code=0`、`data=true`、`success=true`。
- 自动领取开启但任务池无可领取数据时的响应体：HTTP `200`，业务 `code=500`、`message="暂无待标注数据"`、`data=null`、`success=false`，随后返回首页。

用户已说明：后续新对话开始时，不需要再执行“全包填充为其他方言或语种”，因为答案已经由用户处理完成。下一轮只需要采集仍未完成且有实际价值的网络请求。

## 下次优先采集

1. 自动领取开启时提交后的成功路径
   - 目标：读取提交后 `POST /api/v1/label/center/<REDACTED_TASK_ID>/label/fetch` response body。
   - 记录：是否返回新 `subTaskId`、是否进入新详情页、详情页 URL、后续初始化请求。

## 可跳过项

- 全包答案填充：用户已完成，不再重复。
- 异常路径：用户说明实际中一般不会出现，可暂不测试。
- 网络断开模拟：用户说明不符合实际使用场景，可暂不测试。
- 回答区数据筛选 `filter.questions`：用户说明实际使用中基本不会使用，不再采集。
- 音频加载失败：依赖异常资源或网络异常，实际使用中基本不会出现，不再采集。
- 保存失败：需要构造异常或网络失败，实际使用中基本不会出现，不再采集。
- 真实用户快捷键上下题：用户确认不会产生业务请求，不再复测 Network。
- 自动领取关闭时提交：用户说明页面工作流与本次提交后自动领取空池返回首页一致，不再单独复测。

## 脱敏提醒

- 不记录完整 cookie、token、authorization、session、csrf。
- 不记录人员信息、完整音频签名 URL、完整 ASR 文本。
- `projectId`、`taskId`、`subTaskId`、`batchId`、`dataId`、`userId`、`traceId` 使用 `<REDACTED_*>`。
- 保存/提交 payload 只保留字段结构和少量枚举值示例。
