# 待补采网络项

## 登录链

- 未在本轮重新手输账号密码；登录表单 DOM 与提交前页面状态仍待单独补采
- `captcha/login` 与 `user/login` 只保留了请求/响应结构，未补录表单字段来源节点

## 壳层与列表页

- `消息` 面板的真实拉数接口尚未触发
- `个人账户` 页面未进入
- `质检任务`、`验收任务`、`作业统计`、`包管理` 未单独采集

## 作业与编辑器

- `领取` 未触发
- `修改` 未触发，不确认是否与 `继续作业` 共用同一路由
- 编辑器静态音频/波形资源请求未入正文，后续如要做音频控制需单独采集签名策略
- `annotation/postil_list` 只看到空数组，缺少非空样例
- `annotation/check_script` 只看到空 `entry_indexes` 场景，缺少局部条目校验样例

## 写操作

- `保存`
- `挂起`
- `提交`
- `进入修改`
- `退出修改`
- `抽查`
- `自动领取`
- `仅查看`

以上动作当前都应继续视为 `write-action`，在明确授权前不做真实触发采集。
