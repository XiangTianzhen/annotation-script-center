# Playwright-Edge 复核记录（客家话审核页填入）

- 日期：2026-05-26
- 页面：`#/asrmarkCheck?formType=1&id=3373750`
- 目标：验证审核页行内 `填入本行` 与 `全部填入AI推荐`（文本项）能力

## MCP 执行结果（本轮）

1. 已执行 `playwright-edge` 导航到目标页面。
2. 当前会话被重定向到 `https://work.magicdatatech.com/#/login`。
3. 因未持有可用登录态，本轮未完成登录后交互点击复测。

## 额外探测

- options 页（hakka/minnan）可正常打开到扩展设置 URL。
- `#magic-data-review-mode` 未找到，符合“移除 AI 质检模式 UI”目标。

## 本轮代码修复点

1. `extension/sites/magic-data/hakka-helper/assistant-panel.js`
   - 审核页不再全局禁用行内填入按钮。
   - 行内建议块在检测到可编辑文本行时显示 `填入本行`。
   - `全部填入AI推荐` 在审核页仅对文本项生效（客家话/普通话），不自动填说话人。
   - 保持安全边界：不自动保存、不自动提交、不自动点击合格/不合格。

2. `extension/sites/magic-data/shared/data-collector.js`
   - 延续 `contenteditable` 文本节点填入链路，审核页与标注页复用同一填入实现。

## 人工复测矩阵（登录后执行）

1. 打开审核页并启用客家话助手。
2. 点击 `AI 质检当前条`。
3. 若客家话建议可改，第一行下方应出现 `填入本行`。
4. 若普通话建议可改，第二行下方应出现 `填入本行`。
5. 点击第一行 `填入本行`：仅第一行文本变化。
6. 点击第二行 `填入本行`：仅第二行文本变化。
7. 点击 `全部填入AI推荐`（若显示）：仅文本项被填入，不触发保存/提交/合格/不合格。
8. 操作后提示包含：`未保存、未提交`。
9. 结果展示 5 秒内不自动清空。

## 安全说明

- 本记录未包含 token、cookie、authorization、完整签名 URL。
- 本能力仅辅助填入，不改变平台提交边界。
