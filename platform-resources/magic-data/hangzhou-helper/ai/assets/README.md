# Hangzhou Helper AI Assets

当前目录仅作为 `magic-data/hangzhou-helper` 的 AI 资产占位目录。

本轮口径：

- Prompt、schema、rules 与 defaults 继续直接维护在 `backend/ai-*.js`。
- 暂不生成独立资产文件。
- 暂不接入实际杭州话词表文件；词表说明统一查看 `backend/lexicon/README.md`。
