# Legacy Alias（Magic Data Annotator）

此目录仅保留兼容入口：

- `backend/index.js` 会转发到 `../hakka-helper/backend/`。

请优先使用新目录：

- `platform-resources/magic-data/hakka-helper/`
- `platform-resources/magic-data/minnan-helper/`
