# Magic Data 运行时

当前只注入杭州话脚本：

- `hangzhou-helper/`：AI 质检客户端、快捷键、助手面板与 content runtime。
- `shared/page-detector.js`：页面识别。
- `shared/data-collector.js`：当前条数据采集。
- `shared/page-world/network-observer.js`：主世界 Network observer。

运行时不自动保存、不自动提交；用户必须在页面中人工确认。
